# Dockerfile.dev for client development
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Run Vite in development mode, listening on all interfaces
CMD ["npm", "run", "dev", "--", "--host"]
